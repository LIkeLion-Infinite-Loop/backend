name: Deploy to Oracle VM

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.VM_SSH_KEY }}

      - name: Deploy to Oracle VM
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          port: 22
          script: |
            cd ~/backend/sejonghack
            git pull origin develop

            # 환경변수 설정
            export DB_URL="jdbc:mysql://40.233.103.122:3306/sejongdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=UTC"
            export DB_USER="jun"
            export DB_PASS="CgD)@voN%_4~tDsiWPLhb"

            ./gradlew build -x test
            pkill -f 'java -jar' || true
            nohup java -jar build/libs/sejonghack-0.0.1-SNAPSHOT.jar > nohup.out 2>&1 &
