name: Deploy to VM

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy over SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          port: 22
          script: |
            echo "이동: backend/sejonghack"
            cd ~/backend/sejonghack

            echo "코드 최신화"
            git pull origin develop

            echo "프로젝트 빌드 (테스트 제외)"
            ./gradlew build -x test

            echo "기존 프로세스 종료"
            pkill -f 'java -jar' || true

            echo "백그라운드 실행 시작"
            DB_URL="jdbc:mysql://40.233.103.122:3306/sejongdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=UTC" \
            DB_USER="jun" \
            DB_PASS="CgD)@voN%_4~tDsiWPLhb" \
            nohup java -jar build/libs/sejonghack-0.0.1-SNAPSHOT.jar > nohup.out 2>&1 & echo $! > pid.txt

            echo "배포 완료"
